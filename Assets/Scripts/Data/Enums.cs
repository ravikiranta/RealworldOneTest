namespace Enums
{
    public enum PlayerID
    {
        First = 0,
        Second
    }

    public enum KitchenInteractions
    {
        Store,
        Retrieve,
        Chop,
        Combine,
        Deliver
    }

    public enum ItemType
    {
        Item,
        Food
    }
}